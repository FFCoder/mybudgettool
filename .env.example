DATABASE_URL=postgres://budget:budget_local_only@localhost:55439/budget
# Generate a secret with: openssl rand -hex 32
API_TOKEN=replace-with-a-long-random-secret
HOST=127.0.0.1
PORT=3000
BUDGET_API_URL=http://127.0.0.1:3000
# CLI uses API_TOKEN locally; set BUDGET_API_TOKEN to override it.
